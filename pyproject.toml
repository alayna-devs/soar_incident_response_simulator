[project]
name = "incident-response-analyzer"
version = "0.1.0"
description = "A Python SOAR-style incident response automation simulator"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"